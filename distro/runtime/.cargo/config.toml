# Cross-compilation for the embedded image. Requires gcc-aarch64-linux-gnu,
# which images/img/build-img.sh installs as part of its prerequisites.
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"

[alias]
xbuild-arm = "build --release --target aarch64-unknown-linux-gnu"
